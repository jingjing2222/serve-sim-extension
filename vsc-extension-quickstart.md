# VS Code Extension Quickstart

This repo was aligned to the `yo code` TypeScript + esbuild scaffold.

## Run

1. Install dependencies with `pnpm install`.
2. Press `F5`.
3. Run `Serve Sim: Start` or `Serve Sim: Open Panel`.

## Test

```sh
pnpm test
pnpm test:extension
```
